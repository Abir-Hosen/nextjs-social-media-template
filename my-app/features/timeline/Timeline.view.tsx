import { FunctionComponent } from "react";

export interface TimelineViewProps {
    children?: never;
}

export const TimelineView: FunctionComponent<TimelineViewProps> = () => {
    return (
        <>
            Here is timeline details view with components
        </>
    )
}